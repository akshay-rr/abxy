CREATE TABLE TIME_PEN(
  ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  NAME TEXT, TYPE TEXT, TARGET_TIME INT,
  MULTIPLIER INT, UPPER_BOUND INT, UID INT,
  FOREIGN KEY(UID) REFERENCES USERS(ID)) AUTO_INCREMENT=1;
